/**
 * @typedef {Object} AppReducerType
 * @property {string} language
 * @property {(language: string) => void} changeLanguage
 * @property {(theme: string) => void} changeTheme
 * @property {string} theme
 * @property {boolean} showSidebar
 * @property {() => void} toggleSidebar
 */

/**
 * @typedef {Object} DispatchAppLanguageType
 * @property {string} type
 * @property {string} payload
 */

module.exports = {};
